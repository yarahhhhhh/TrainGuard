# TrainGuard

## Description
TrainGuard is a Python-based project.

## Setup
1. Clone the repository.
2. Install dependencies: `pip install -r requirements.txt`
3. Run the application: `python main.py`
